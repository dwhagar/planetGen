[pytest]
testpaths = src/tests
# src/ needs to be on the path for `import stellarObjects`/`import api`
# (src layout); "." for `import queryDb`/`sectorGen`/`systemGen`, the
# top-level entry scripts that stay at the repo root.
pythonpath = . src
